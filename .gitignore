# Dependencies - installed from package.json, never committed
node_modules/ # Node.js third-party libraries - huge, can be regenerated by 'npm install'
# Environment files - may contain secrets or local configuration
.env # The main file containing actual secrets (never commit this)
.env.* # Any environment files (e.g., .env.development, .env.production)
# Logs and caches - noisy files created at runtime or by development tools
*.log # Any file ending in .log
logs/ # A common directory for log files
npm-debug.log # Debug file created by npm (package manager)
.eslintcache # Cache file for ESLint (code linter)
# Build outputs and coverage - files that can be regenerated from source code
dist/ # Common directory for distribution/production ready code
build/ # Another common directory for built output
coverage/ # Test coverage reports
# OS and editor artefacts - machine specific files that don't belong in Git
.DS_Store # macOS file system metadata
Thumbs.db # Windows file system metadata
.vscode/ # Visual Studio Code settings for this workspace
.idea/ # IntelliJ IDEA IDE settings for this workspace
